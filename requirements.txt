# Stage 2 — embedding model. Downloads once (~90MB), then runs locally on CPU. No API, no cost.
sentence-transformers>=2.2.0

# Stage 3 — vector index + nearest-neighbor search (exact, IndexFlatIP at this scale).
faiss-cpu>=1.7.4

# Stage 5 — LLM reasoning. Google Gemini free tier.
# Free API key (no credit card): https://aistudio.google.com/apikey
google-genai>=0.3.0
